#!/usr/bin/env python3
# -*- coding: utf-8 -*-

# chapitre-02 – Jenga Workspace
# Generated by `jenga workspace` on 2026-09-24 15:41:17

from Jenga import *
import sys


print("configuration :", ["Debug", "Release"], file=sys.stderr)


with workspace("chapitre-02"):
    configurations(['Debug', 'Release'])
    targetoses([TargetOS.WINDOWS])
    targetarchs([TargetArch.X86_64])

    
    # Default toolchain (auto-detected)
    # usetoolchain("host-gcc")
    
    # Uncomment to use Unitest testing framework
    # with unitest() as u:
    #     u.Precompiled()
    
    # Add your projects here
    # with project("MyApp"):
    #     consoleapp()
    #     language("C++")
    #     files(["src/**.cpp"])

    # Project: exo1-l_etape_1_chez_vous (fichier .jenga separe, inclus)
    with include("exo1-l_etape_1_chez_vous/exo1-l_etape_1_chez_vous.jenga"):
        pass

    # Project: exo2-le_workspace_retire
    with project("exo2-le_workspace_retire"):
        consoleapp()
        language("C++")
        location("exo2-le_workspace_retire")
        files(["src/**.cpp", "include/**.hpp"])


    # Project: exo3-les_trois_erreurs_provoquees
    # Le programme qui s'en sert.
    with project("Essai"):
        consoleapp()
        language("C++")
        cppdialect("C++17")
        location("exo3-les_trois_erreurs_provoquees")
        files(["src/**.cpp", "include/**.hpp"])
        includedirs(["src"])  
        """ libdirs(["%{wks.location}/Build/Lib/"
                 "%{cfg.buildcfg}-%{cfg.system}"]) """
        links(["MonRhi"]) 
        objdir("%{wks.location}/Build/Obj/"
               "%{cfg.buildcfg}-%{cfg.system}/%{prj.name}")
        targetdir("%{wks.location}/Build/Bin/"
                  "%{cfg.buildcfg}-%{cfg.system}/%{prj.name}")

    # Project: MonRhi
    with project("MonRhi"):
        staticlib()
        language("C++")
        location("MonRhi")
        files(["src/**.cpp", "include/**.hpp"])
        includedirs(["src"])
        objdir("%{wks.location}/Build/Obj/"
               "%{cfg.buildcfg}-%{cfg.system}/%{prj.name}")
        targetdir("%{wks.location}/Build/Lib/"
                  "%{cfg.buildcfg}-%{cfg.system}")

    # Project: exo4-le_print_dans_le_fichier_de_projet
    with project("exo4-le_print_dans_le_fichier_de_projet"):
        consoleapp()
        language("C++")
        location("exo4-le_print_dans_le_fichier_de_projet")
        files(["src/**.cpp", "include/**.hpp"])
